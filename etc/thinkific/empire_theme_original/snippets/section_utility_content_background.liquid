{% if section.settings.background_image != blank or section.settings.overlay_opacity > 0 %}
  section-content-background
  {% if section.settings.overlay_opacity > 0 %}
    section-background-overlay
  {% else %}
    section-background-image
  {% endif %}
{% endif %}
